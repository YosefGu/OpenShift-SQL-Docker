

oc new-app \
    -e MYSQL_ROOT_PASSWORD=myrootpass \
    -e MYSQL_USER=username \
    -e MYSQL_PASSWORD=password \
    -e MYSQL_DATABASE=database \
    quay.io/sclorg/mysql-80-c9s

oc expose service/mysql<id>

oc get pods

oc rsh <pod id>

mysql CREATE TABLE users (id int, first_name varchar(255), last_name varchar(255));

oc new-app https://github.com/YosefGu/OpenShift-SQL-Docker

oc get deployments

oc set env deployments/<deployment-name> 